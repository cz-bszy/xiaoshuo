---
name: mega-novel-orchestrator
description: "超长篇小说（100万字+）自动化写作核心调度器。当用户要创建大规模小说项目、需要自动化写作流程、或提到长篇/百万字/多卷时自动激活。提供项目初始化、自动写作循环、多模型协调、进度监控等功能。"
allowed-tools: Read, Edit, Grep, Terminal
---

# 超长篇小说自动化写作调度器

## 核心功能

**Mega Novel Orchestrator** 是专为100万字以上超长篇小说设计的自动化写作系统。

### 设计规模

| 规模 | 字数 | 卷数 | 推荐使用 |
|------|------|------|---------|
| 大长篇 | 100-200万 | 4-8卷 | 本系统 |
| 超长篇 | 200-400万 | 8-16卷 | 本系统（完整功能） |
| 巨长篇 | 400万+ | 16卷+ | 本系统 + 人工分阶段 |

---

## 系统架构

```
mega-novel-orchestrator（你正在看的）
├── progress-tracker      # 进度追踪
├── quality-monitor       # 质量监控
├── outline-manager       # 大纲管理
├── consistency-checker-mega  # 增强一致性
└── [类型知识库]          # 如 western-fantasy-farming
```

### 双模型协作

```
┌─────────────────┐     ┌─────────────────┐
│   写作模型       │     │   监控模型       │
│  (DeepSeek等)   │     │  (Claude/当前)   │
│                 │     │                 │
│  负责：          │     │  负责：          │
│  - 章节创作      │     │  - 质量评估      │
│  - 对话生成      │     │  - 逻辑检查      │
│  - 细节描写      │     │  - 方向把控      │
└────────┬────────┘     └────────┬────────┘
         │                       │
         └───────────┬───────────┘
                     │
              ┌──────▼──────┐
              │  调度器      │
              │ (本Skill)   │
              └─────────────┘
```

---

## 命令列表

### 初始化命令

| 命令 | 功能 | 使用时机 |
|------|------|---------|
| `/mega-init` | 初始化项目 | 开始新项目 |
| `/mega-config` | 配置API和参数 | 首次设置 |
| `/mega-constitution` | 创作宪法 | 项目启动 |
| `/mega-specify` | 故事规格 | 项目启动 |
| `/mega-outline` | 多层大纲 | 项目启动 |
| `/mega-worldbook` | 初始化世界书 | 项目启动 |

### 写作命令

| 命令 | 功能 | 使用时机 |
|------|------|---------|
| `/mega-start` | 开始自动写作 | 准备就绪后 |
| `/mega-pause` | 暂停写作 | 需要干预时 |
| `/mega-resume` | 恢复写作 | 暂停后继续 |
| `/mega-write [章节]` | 写指定章节 | 手动模式 |

### 监控命令

| 命令 | 功能 | 使用时机 |
|------|------|---------|
| `/mega-status` | 查看当前状态 | 随时 |
| `/mega-progress` | 详细进度报告 | 定期检查 |
| `/mega-quality` | 质量评估报告 | 每卷/每20章 |
| `/mega-issues` | 问题清单 | 发现问题时 |

### 调整命令

| 命令 | 功能 | 使用时机 |
|------|------|---------|
| `/mega-adjust` | 调整后续大纲 | 需要修正方向 |
| `/mega-revise [章节]` | 修订指定章节 | 发现问题 |
| `/mega-rewrite [范围]` | 重写章节范围 | 重大问题 |

---

## 详细命令说明

### `/mega-init` - 项目初始化

**触发**：用户说"创建新项目"、"开始写小说"、"初始化"

**执行步骤**：

1. 创建项目目录结构
2. 生成配置文件模板
3. 初始化状态文件
4. 提示下一步

**项目结构**：

```
projects/[项目名]/
├── config.yaml           # 项目配置
├── project-state.json    # 项目状态
├── constitution.md       # 创作宪法
├── specification.md      # 故事规格
├── outline/              # 大纲目录
│   ├── L0-main.md       # 总纲
│   ├── L1-volumes/      # 卷纲
│   ├── L2-parts/        # 篇纲
│   └── L3-chapters/     # 章纲
├── chapters/             # 章节目录
│   └── v01/             # 按卷组织
├── worldbook/           # 世界书
│   ├── characters.json  # 人物库
│   ├── locations.json   # 地点库
│   ├── items.json       # 物品库
│   ├── events.json      # 事件库
│   └── rules.json       # 规则库
├── logs/                # 日志
└── backups/             # 备份
```

### `/mega-config` - 配置系统

**配置项**：

```yaml
# config.yaml 模板

project:
  name: "我的小说"
  target_words: 4000000      # 目标字数
  words_per_chapter: 3000    # 每章字数
  chapters_per_volume: 100   # 每卷章节数
  
models:
  writing:
    provider: deepseek
    model: deepseek-chat
    api_key: ${DEEPSEEK_API_KEY}
    api_base: https://api.deepseek.com
    temperature: 0.8
    max_tokens: 4000
    
  # 监控由当前对话界面担任，无需API配置
    
automation:
  auto_write_batch: 5        # 每批自动写作章节数
  quality_check_interval: 5  # 质量检查间隔（章）
  auto_save: true
  backup_interval: 10        # 备份间隔（章）
  
quality:
  min_words_per_chapter: 2500
  max_words_per_chapter: 4000
  consistency_check: true
  style_check: true
```

### `/mega-start` - 开始自动写作

**前置条件**：
- [x] 项目已初始化
- [x] 创作宪法已完成
- [x] 故事规格已完成
- [x] 至少L0-L2大纲已完成
- [x] API配置正确

**自动写作循环**：

```
开始
 │
 ▼
┌─────────────────────────────┐
│ 1. 获取下一章任务            │
│    - 读取章纲               │
│    - 加载相关世界书          │
│    - 准备上下文（前N章摘要）  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 2. 调用写作模型(DeepSeek)    │
│    - 发送写作Prompt          │
│    - 获取章节内容            │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 3. 快速检查                  │
│    - 字数检查               │
│    - 基本格式检查            │
│    - 关键词一致性            │
└──────────────┬──────────────┘
               │
               ▼
         ┌─────┴─────┐
         │  通过？    │
         └─────┬─────┘
          是 ↙   ↘ 否
           │       │
           │       ▼
           │    ┌──────────┐
           │    │ 自动修正  │
           │    └────┬─────┘
           │         │
           ▼         ▼
┌─────────────────────────────┐
│ 4. 保存章节                  │
│    - 写入文件               │
│    - 更新状态               │
│    - 更新世界书              │
└──────────────┬──────────────┘
               │
               ▼
         ┌─────┴─────┐
         │ 达到检查点？│ (每5章)
         └─────┬─────┘
          是 ↙   ↘ 否
           │       │
           ▼       │
┌──────────────┐   │
│ 5. 质量评估   │   │
│ (监控界面)   │   │
└──────┬───────┘   │
       │           │
       ▼           │
  ┌────┴────┐      │
  │需要调整？ │      │
  └────┬────┘      │
   是 ↙ ↘ 否       │
    │     │        │
    ▼     └────────┤
┌────────┐         │
│调整大纲 │         │
└────┬───┘         │
     │             │
     └─────────────┤
                   │
                   ▼
            ┌──────┴──────┐
            │ 继续下一章？  │
            └──────┬──────┘
              是 ↙   ↘ 否
               │       │
               ↑       ▼
               │    结束/暂停
               │
         (循环回步骤1)
```

### `/mega-status` - 状态查看

**输出示例**：

```
📊 项目状态报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 项目：我的西幻种田小说
🎯 目标：4,000,000 字 / 1334 章 / 13 卷

📈 当前进度
├── 已完成：523,000 字 (13.1%)
├── 已写章节：175 / 1334
├── 当前卷：第2卷 (进度 75/100)
└── 预计完成：2026年8月

⏱️ 写作状态：运行中
├── 当前任务：第2卷第76章
├── 本次会话：已写 12 章
└── 上次保存：2分钟前

📋 质量状态
├── 整体评分：8.2/10
├── 一致性：✅ 通过
├── 节奏：✅ 正常
└── 待处理问题：2个

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 与其他模块协作

### progress-tracker

- 接收写作进度更新
- 提供进度统计数据
- 生成进度可视化

### quality-monitor

- 每N章触发质量评估
- 接收评估结果
- 根据结果决定是否暂停

### outline-manager

- 获取当前章节大纲
- 请求动态调整大纲
- 同步大纲变更

### consistency-checker-mega

- 写作时请求一致性检查
- 获取世界书数据
- 更新世界书

---

## 自动化工作流

### 批量写作模式

```python
# 伪代码示意

async def auto_write_batch(batch_size: int = 5):
    for i in range(batch_size):
        # 1. 准备章节任务
        task = await outline_manager.get_next_chapter()
        context = await prepare_context(task)
        
        # 2. 调用写作模型
        content = await writing_model.generate(
            prompt=build_prompt(task, context),
            temperature=0.8
        )
        
        # 3. 快速检查
        if not quick_check(content):
            content = await writing_model.revise(content)
        
        # 4. 保存
        await save_chapter(task.chapter_id, content)
        await update_worldbook(content)
        await progress_tracker.update()
        
    # 5. 批次完成后质量评估
    report = await quality_monitor.evaluate_batch()
    
    if report.needs_adjustment:
        await notify_user(report)
        # 等待用户确认
    else:
        # 继续下一批
        await auto_write_batch(batch_size)
```

---

## 最佳实践

### 1. 项目启动

1. 先完成创作宪法和故事规格
2. 至少完成前3卷的详细大纲
3. 建立完整的世界书基础
4. 测试写3-5章验证流程

### 2. 写作过程

1. 每5章暂停检查质量
2. 每卷完成后全面评估
3. 发现问题及时调整
4. 定期备份

### 3. 质量控制

1. 保持大纲灵活性（L3层可随时调整）
2. 世界书持续更新
3. 重大问题回溯修正
4. 保持日常种田/事件的合理比例

---

## 脚本位置

本Skill的自动化脚本位于：

```
skills/mega-novel-orchestrator/mega-novel-orchestrator/scripts/
├── orchestrator.py      # 核心调度
├── api_client.py        # API调用
├── context_builder.py   # 上下文构建
├── chapter_manager.py   # 章节管理
└── prompt_templates.py  # Prompt模板
```

---

**版本**：v1.0
**更新**：2026-01-25
**依赖**：progress-tracker, quality-monitor, outline-manager, consistency-checker-mega
